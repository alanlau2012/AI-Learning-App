# AI ??????????

HyperFrames ????????

`eval -> trace -> observability -> ops-diagnosis-agent -> sla`

## Preview

```bash
npx --yes hyperframes lint materials/hyperframes/eval-trace-observability
npx --yes hyperframes validate materials/hyperframes/eval-trace-observability
npx --yes hyperframes inspect materials/hyperframes/eval-trace-observability --samples 15
npx --yes hyperframes preview materials/hyperframes/eval-trace-observability --port 3022
```

Studio URL:

```text
http://localhost:3022/#project/eval-trace-observability
```

## Notes

- This is an external HyperFrames experiment and is not imported by the app.
- No narration or audio is included.
- MP4 rendering needs FFmpeg available on PATH.
