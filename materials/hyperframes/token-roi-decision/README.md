# Token ROI???????????????

HyperFrames ????????

`token -> cost-routing -> token-roi -> value-review-agent -> eval -> kv-cache`

## Preview

```bash
npx --yes hyperframes lint materials/hyperframes/token-roi-decision
npx --yes hyperframes validate materials/hyperframes/token-roi-decision
npx --yes hyperframes inspect materials/hyperframes/token-roi-decision --samples 15
npx --yes hyperframes preview materials/hyperframes/token-roi-decision --port 3023
```

Studio URL:

```text
http://localhost:3023/#project/token-roi-decision
```

## Notes

- This is an external HyperFrames experiment and is not imported by the app.
- No narration or audio is included.
- MP4 rendering needs FFmpeg available on PATH.
