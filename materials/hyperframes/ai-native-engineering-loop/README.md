# AI ????????

HyperFrames ????????

`agents-md -> repo-context -> spec-driven-development -> issue-fix-agent -> code-review-agent -> test-generation-agent -> value-review-agent`

## Preview

```bash
npx --yes hyperframes lint materials/hyperframes/ai-native-engineering-loop
npx --yes hyperframes validate materials/hyperframes/ai-native-engineering-loop
npx --yes hyperframes inspect materials/hyperframes/ai-native-engineering-loop --samples 15
npx --yes hyperframes preview materials/hyperframes/ai-native-engineering-loop --port 3021
```

Studio URL:

```text
http://localhost:3021/#project/ai-native-engineering-loop
```

## Notes

- This is an external HyperFrames experiment and is not imported by the app.
- No narration or audio is included.
- MP4 rendering needs FFmpeg available on PATH.
