# ?????????

HyperFrames ????????

`permission-governance -> human-in-the-loop -> tool-calling -> trace -> observability -> ai-native-org`

## Preview

```bash
npx --yes hyperframes lint materials/hyperframes/permission-human-gate
npx --yes hyperframes validate materials/hyperframes/permission-human-gate
npx --yes hyperframes inspect materials/hyperframes/permission-human-gate --samples 15
npx --yes hyperframes preview materials/hyperframes/permission-human-gate --port 3024
```

Studio URL:

```text
http://localhost:3024/#project/permission-human-gate
```

## Notes

- This is an external HyperFrames experiment and is not imported by the app.
- No narration or audio is included.
- MP4 rendering needs FFmpeg available on PATH.
