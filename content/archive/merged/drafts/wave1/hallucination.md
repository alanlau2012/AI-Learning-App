# hallucination：幻觉

## 一句话定义
幻觉是模型生成了看似合理但缺少事实依据、与上下文冲突或无法被系统证据支持的内容。

## 为什么重要
幻觉不是单纯“模型胡说”，而是生成式系统在证据、约束、采样、上下文和任务边界失效时的外显症状。企业用户看到的是错误引用、编造政策、虚构 API、错误操作建议或自信的误判。平台侧要看的指标包括引用覆盖率、无证据回答率、检索命中率、冲突证据处理、拒答/升级率和人工复核通过率。

## 心智模型
模型像一个非常擅长续写的人：当证据足够、约束清楚时，它能把材料组织成好答案；当证据缺失或冲突时，它仍可能沿着语言模式补出一个顺滑结论。幻觉治理的重点不是要求模型“更诚实”这一句口号，而是让系统知道什么时候有证据、什么时候该停下、什么时候该请求检索或人工。

## 机制拆解
1. 自回归生成会不断选择下一个 token，语言连贯性不等于事实正确性。
2. 当上下文缺少证据、检索片段错误或提示词要求必须回答时，模型更容易补全不存在的信息。
3. 高随机采样、长输出和复杂多步任务会增加无证据扩写机会。
4. RAG 系统中的幻觉常来自召回错误、引用未校验、版本冲突或片段缺失。
5. Agent 场景中的幻觉可能表现为虚构工具结果、误读日志、编造文件路径或错误判断任务完成。

## 动画脚本或无动画说明
本讲为纯文本内容，不新增动画。幻觉治理依赖证据链和评测闭环，本轮不硬接动画。

## 企业案例
**标题：运维助手编造不存在的配置项**

- 场景：某云平台运维助手接入日志、告警和内部 runbook，每天处理约 6000 次排障咨询。
- 问题：一次数据库连接池告警中，助手建议修改一个并不存在的参数名，值班工程师照做失败，导致恢复时间增加 18 分钟。
- 分析：检索没有命中对应版本 runbook，模型根据相似产品文档补全了看似合理的参数；系统没有要求引用配置来源，也没有对工具查询失败设置拒答或升级。
- 方案：强制关键操作建议绑定 runbook 版本和配置路径；无证据时输出“需要人工确认”；将不存在参数、跨版本参数加入评测集。
- 结论：幻觉治理要围绕证据、版本、工具结果和升级路径建立系统约束，而不是只在提示词里要求“不要编造”。

工程信号：规模、错误路径、约束条件、指标、验证结果。

## 常见误区
1. 认为把 temperature 调低就能消除幻觉。
2. 要求模型“不要编造”，但没有证据引用、检索校验和拒答路径。
3. 把幻觉全部归因于模型能力，不排查 RAG 召回、版本和权限过滤。
4. 只看回答是否流畅，不记录引用覆盖率和人工复核结果。
5. Agent 工具失败后仍让模型继续总结，导致虚构工具结果。

## 诊断题
**场景**：运维助手在某次告警中建议修改不存在的配置项。日志显示检索没有命中当前版本 runbook，但模型仍输出了具体参数名和修改值。当前提示词要求“尽量给出可执行修复步骤”。

**问题**：最应该优先修复哪一层？

A. 降低 temperature，并保持当前“尽量给出步骤”的产品要求  
B. 增加更多通用运维文档，让模型拥有更广知识  
C. 为关键操作建立证据引用、版本校验和无证据升级路径  
D. 把回答改得更保守，但不改变检索和工具链路

**正确答案**：C

**解析**：C 直接修复幻觉的证据链和系统边界。A 只能降低随机性，不能保证不存在参数不被稳定编造。B 是强干扰项，更多文档可能带来更多跨版本噪声。D 改语气不能解决无证据仍输出操作建议的问题。

**排查路径**：
1. 查看错误建议是否有引用来源和版本。
2. 检查检索未命中时的生成策略。
3. 对关键操作要求工具查询或 runbook 引用。
4. 设置无证据时的拒答、补检索或人工升级。
5. 建立不存在参数和版本冲突的回归评测。

## 核心结论
1. 幻觉是缺证据、错上下文、弱约束和生成机制共同作用的结果。
2. 流畅、自信和详细都不能证明答案正确。
3. RAG 场景要治理召回、引用、版本和证据覆盖。
4. Agent 场景要防止虚构工具结果和错误完成判断。
5. 企业治理应使用评测、trace、人工复核和升级路径闭环。

## 关联知识点
autoregressive, sampling, attention, context-window, rag-pipeline, eval, trace

