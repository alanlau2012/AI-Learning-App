# session-affinity 审核结论：PASS

## 1. 诊断题门禁
通过。正确答案 B；题目考察扩容后会话打散、KV 命中下降和 TTFT 抖动的优先排查。A 为强干扰项，按空闲实例分配看似均衡但会破坏亲和；C/D 与现象不匹配。

## 2. 结构门禁
通过。包含定义、重要性、心智模型、5 条机制、4 条误区、4 条核心结论。心智模型用“案卷/柜台”解释缓存和状态连续，不是模板化句式。

## 3. 企业案例门禁
通过。包含规模、指标、系统边界、错误路径、约束条件、验证结果，可复盘“扩容 -> 会话迁移 -> KV 未命中 -> 亲和和预热”的链路。

## 4. 动画门禁
通过。复用 `kv-cache`，不新增动画协议或组件。highlightTargets：`session`, `instance`, `kv-write`, `cache-hit`, `route-miss`, `cache-miss`, `memory`, `eviction`，均能映射到 KV Cache 画布的会话、实例、命中/未命中路径和容量元素。

## 5. schema 门禁
通过。入库应设置 `hasAnimation: true` 且 `animation.type = "kv-cache"`；关联 id 均已登记。

## 6. 合并注意事项
动画步骤应强调“亲和命中”和“打散重算”对比，同时保留过度亲和的热点/淘汰风险。

