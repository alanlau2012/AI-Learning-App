/**
 * 内容数据 schema 的唯一来源（权威）。
 *
 * 完整类型将在后续阶段按 docs/content-schema.md §1 落入本文件，包括：
 * KnowledgePoint / LearningModule / AnimationConfig / AnimationStep /
 * EnterpriseCase / DiagnosticQuestion / DiagnosticOption / UserProgress /
 * GlossaryTerm，以及 Difficulty / ContentStatus / AnimationType 枚举。
 *
 * 当前为骨架占位，暂不定义任何接口。
 */
export {};
