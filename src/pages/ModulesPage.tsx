/**
 * 模块总览（/modules）—— P3 实现。P2 先占位。
 */
import { PagePlaceholder } from '../components/layout/PagePlaceholder';

export function ModulesPage() {
  return (
    <PagePlaceholder
      stage="P3"
      title="模块总览"
      intent="六大模块的路径说明与完成进度将在此展示；可在左侧栏或下方直接进入任一模块。"
    />
  );
}

export default ModulesPage;
